FROM quay.io/fedora/fedora-minimal:45

# Install Python (uses stdlib + /proc for memory monitoring)
RUN microdnf install -y python3.13 && \
    microdnf clean all && \
    rm -rf /var/cache/yum

COPY --chmod=755 main.py .

EXPOSE 80
USER 1001

CMD ["python3", "main.py"]