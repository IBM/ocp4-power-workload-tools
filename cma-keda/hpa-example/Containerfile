FROM quay.io/fedora/fedora-minimal:45

# Install Python (no pip needed - only uses stdlib)
RUN microdnf install -y python3.13 && \
    microdnf clean all && \
    rm -rf /var/cache/yum

COPY --chmod=755 main.py .

EXPOSE 80
USER 1001

CMD ["/usr/bin/python3.13", "main.py"]